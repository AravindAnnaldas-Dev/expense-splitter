DATABASE_URL="postgresql://postgres:postgres@localhost:5432/expense_splitter?schema=public"
JWT_SECRET="d09a4d45525b411dbf2926b0bfa4686b4ea6614c8352af1cc00e5d9e54eb272"
PORT=4000
CLIENT_ORIGIN="http://localhost:3000"
# Controls auth cookie attributes (Secure+SameSite=None vs plain Lax).
# Set to "true" on any real deployment where the client and server are on
# different domains (e.g. Vercel + Railway) — leave unset/false for local
# dev over plain http, or browsers will silently refuse to send the
# cookies at all.
COOKIE_SECURE="false"
