# Local dev only — client and server are both "localhost" (same site), so
# auth cookies work talking directly to the server, no proxy needed.
# Leave this UNSET in production (see next.config.mjs + API_PROXY_TARGET).
NEXT_PUBLIC_API_URL=http://localhost:4000
